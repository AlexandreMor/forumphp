{% extends 'base.html.twig' %}

{% block title %}Edit Topic{% endblock %}

{% block body %}
    <h1>Edition du sujet {{topic.title}}</h1>

    {{ include('topic/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a class="btn btn-light mt-3" href="{{path('home_topic',{name : topic.subcategory.title, id:topic.id})}}">Retour au sujet</a>

    {{ include('topic/_delete_form.html.twig') }}
{% endblock %}
